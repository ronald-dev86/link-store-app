import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.link.store',
  appName: 'link-store-web',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
